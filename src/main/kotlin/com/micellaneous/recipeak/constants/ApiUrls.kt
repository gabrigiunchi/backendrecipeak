package com.micellaneous.recipeak.constants

object ApiUrls {
    private const val BASE_URL = "/api/v1"
    const val USERS = "$BASE_URL/users"
    const val LOGIN = "$BASE_URL/login"
    const val ALIVE = "$BASE_URL/alive"
}